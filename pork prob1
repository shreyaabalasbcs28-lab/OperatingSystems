#include<stdio.h>
#include<unistd.h>
int main(){
   int p2;
   p2=fork();
   if(p2==0){
      int p4;
      p4=fork();
      if(p4==0){
	 printf("P4ID:%d,P4PPID:%d\n",getpid(),getppid());
      }
      else{
	 wait(NULL);
	 printf("P2ID:%d,P2PPID:%d\n",getpid(),getppid());
      }
   }
   else{
      wait(NULL);
      int p3;
      p3=fork();
      if(p3==0){
       	 int p5;
	 p5=fork();
	 if(p5==0){
	    printf("P5ID:%d,P5PPID:%d\n",getpid(),getppid());
	 }
	 else{
	    wait(NULL);
	    printf("P3ID:%d,P3PPID:%d\n",getpid(),getppid());
	 }
      }
      else{
	 wait(NULL);
	 printf("P1ID:%d,P1PPID:%d\n",getpid(),getppid());
      }
   }
   return 0;
}
